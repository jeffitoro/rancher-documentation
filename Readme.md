# Run in local
### ruby --version 2.X
### gem install bundler
### file: Gemfile with 
> source 'https://rubygems.org'
> gem 'github-pages', group: :jekyll_plugins
### bundle install
### > bundle exec jekyll serve --source=docs